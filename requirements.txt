# Core framework - optimized for cloud deployment (Render, Railway, etc.)
langchain>=0.1.0,<0.3.0
langgraph>=0.0.40,<0.3.0
pydantic>=2.5.0,<3.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# LLM providers
langchain-google-genai>=1.0.0     # Google Gemini free tier
langchain-openai>=0.1.0           # OpenAI GPT models (GPT-3.5/4, free credits if available)
langchain-community>=0.0.20       # Ollama support + other integrations

# FREE Search tools 
ddgs>=3.9.0                        # DuckDuckGo Search API
google-search-results>=2.4.2       # SerpAPI free tier

# Database and storage
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Content processing
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0

# Essential utilities
python-dotenv>=1.0.0
httpx>=0.25.0
aiofiles>=23.0.0
typer>=0.9.0
rich>=13.0.0

# Optional: Monitoring (free tier available)
langsmith>=0.0.80
